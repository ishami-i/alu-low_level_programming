#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase and bigcase followed by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int c;
	int b;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
	for (b = 1; c <= ; c++)
		putchar(c);

	putchar('\n');

	return (0);
}
